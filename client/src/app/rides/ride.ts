// import {User} from "../users/user";
//import DateTimeFormat = Intl.DateTimeFormat;

export class Ride {
  _id: string;
  driver: string;
  destination: string;
  origin: string;
  departure: string;
}
